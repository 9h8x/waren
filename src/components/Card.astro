---
interface Props {
	title: string;
	body: string;
	href: string;
}

const { href, title, body } = Astro.props;
---

<div class="group">
	<a href={href} class="block p-6 bg-zinc-900 border border-violet-800/50 rounded-lg hover:border-violet-500 transition-all duration-300">
		<h2 class="text-xl font-semibold mb-2 text-violet-400 group-hover:text-violet-300">
			{title}
			<span class="inline-block transition-transform group-hover:translate-x-1">→</span>
		</h2>
		<p class="text-gray-300">
			{body}
		</p>
	</a>
</div>